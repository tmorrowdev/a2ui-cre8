{
  "name": "a2ui-bridge",
  "version": "0.0.0",
  "private": true,
  "description": "Framework adapters for Google's A2UI protocol - enabling LLM-driven UI generation",
  "author": "tpitre",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/southleft/a2ui-bridge.git"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=18"
  }
}
